#!/usr/bin/env bash

PROJECTS=(
  'dynaru'
  'excon'
  'fog'
  'formatador'
  'gestalt'
  'prototypes'
  'radome'
  'rakegem'
  'shindo'
  'tach'
)

echo "syncing projects..."

for PROJECT in ${PROJECTS[@]}
do
  echo "  ${PROJECT}"
  if [ ! -d ~/geemus/${PROJECT} ]; then
    cd ~/geemus && git clone git@github.com:geemus/${PROJECT} >> /dev/null
  fi
  cd ~/geemus/formatador && git pull >> /dev/null
done
